# 🚨 DISASTER RECOVERY CONFIGURATION 🚨
# Copy this file to .env and configure your source/target RDS instances

# ==========================================
# SOURCE DATABASE (where data currently exists)
# ==========================================
SOURCE_DB_SERVER=your-source-rds-endpoint.rds.amazonaws.com
SOURCE_DB_PORT=1433
SOURCE_DB_DATABASE=your_source_database_name
SOURCE_DB_USER=your_source_username
SOURCE_DB_PASSWORD=your_source_password
SOURCE_DB_ENCRYPT=true
SOURCE_DB_TRUST_SERVER_CERTIFICATE=false
SOURCE_DB_CONNECTION_TIMEOUT=60000
SOURCE_DB_REQUEST_TIMEOUT=120000

# ==========================================
# TARGET DATABASE (where data needs to be restored)
# ==========================================
TARGET_DB_SERVER=your-target-rds-endpoint.rds.amazonaws.com
TARGET_DB_PORT=1433
TARGET_DB_DATABASE=your_target_database_name
TARGET_DB_USER=your_target_username
TARGET_DB_PASSWORD=your_target_password
TARGET_DB_ENCRYPT=true
TARGET_DB_TRUST_SERVER_CERTIFICATE=false
TARGET_DB_CONNECTION_TIMEOUT=60000
TARGET_DB_REQUEST_TIMEOUT=120000

# ==========================================
# MIGRATION SETTINGS
# ==========================================
MIGRATION_EXPORT_PATH=./disaster_recovery_export
VALIDATE_DATA=true
CREATE_BACKUP=true
BACKUP_PATH=./pre_migration_backup
MAX_RETRIES=3
RETRY_DELAY=5000

# ==========================================
# PERFORMANCE TUNING (for large databases)
# ==========================================
MAX_ROWS_PER_FILE=50000
MEMORY_LIMIT=1000000000
ENABLE_STREAMING=true

# ==========================================
# IMPORTANT NOTES:
# ==========================================
# 1. Ensure both RDS instances are accessible from your network
# 2. Verify security groups allow connections on port 1433
# 3. Make sure database users have appropriate permissions
# 4. Test with small dataset first if possible
# 5. Monitor AWS RDS performance during migration
# 6. Consider increasing RDS instance size for large migrations